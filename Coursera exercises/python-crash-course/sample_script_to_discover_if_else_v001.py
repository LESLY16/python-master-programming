#Note that instead of only using return, you can use return None but not the case with return True
def is_positive(number):
  if number>0:
    return True
  else:
    return

print(is_positive(10))
print(is_positive(-5))